Write an application that recursively crawls a website.

The user should also be able to specify a download_delay that each request will respect. Also provide the ability to specify the maximum number of urls to visit. The spider should not visit the same url twice.

In the end display the total number of unique pages visited and bytes downloaded.
